import styled from 'styled-components';
import vars from '../../../vars';

const FormIntro = styled.h3`
 text-align: center;
 font-family: ${vars.fontTitle};
 font-size: 22px;
 letter-spacing: 0.6px;
`;

export default FormIntro;
